[package]
name = "svgo-plugins"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
svgo-core = { path = "../core" }
serde_json = "1"
indexmap = "2"

[dev-dependencies]
svgo-core = { path = "../core" }
